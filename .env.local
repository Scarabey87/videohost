# PostgreSQL подключение (замените на свои данные)
DATABASE_URL="postgresql://postgres:password@localhost:5432/ai_video_platform?schema=public"

# Для локальной разработки без БД (опционально)
NODE_ENV="development"

MOCK_AUTH=true